; ----------------------------------------------------------------------------
; Restore the thread's context.
; ----------------------------------------------------------------------------

_SwapContext:
	move.l 4(sp),a0
	move.l 8(sp),a1
	movem.l d0-d7/a0-a6,(a0)
	movem.l (a1),d0-d7/a0-a6
	rts
